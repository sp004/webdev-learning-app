/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'res.cloudinary.com', 'firebasestorage.googleapis.com', 'images.unsplash.com']
    },
}

module.exports = nextConfig
